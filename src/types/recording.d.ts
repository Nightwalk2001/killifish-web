declare type Recording = {
    tank: string
    owner: string
    genotype?: string
    sexual?: string
    birthday?: string
    quantity: number
    trigger: string
    time: string
    succeed: boolean
}
